# Backend URL used by client components (browser) — must be publicly reachable
NEXT_PUBLIC_API_URL=http://localhost:5000

# Backend URL used by server components (server-side fetch) — can be internal
API_URL=http://localhost:5000
