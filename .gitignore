node_modules
.next
dist
build
# Environment files: ignore everything, keep the checked-in examples.
.env*
!.env.example
!.env.*.example
*.log
.DS_Store
*.pem
.vercel

# TLS certs (Cloudflare Origin cert) — server-local secrets, never commit
infra/certs/

# Personal working notes — list open security gaps, never committed
TODO.md
PENTEST-*.md
